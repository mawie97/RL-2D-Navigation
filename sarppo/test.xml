<mujoco model="pid_step_test">
  <compiler angle="degree" coordinate="local" inertiafromgeom="true"/>
  <option integrator="RK4" timestep="0.01"/>

  <!-- PID plugin with your current gains -->
  <extension>
    <plugin plugin="mujoco.pid">
      <instance name="pid1">
        <!-- EDIT THESE TO TEST DIFFERENT GAINS -->
        <config key="kp" value="2"/>
        <config key="ki" value="0"/>
        <config key="kd" value="55"/>
      </instance>
    </plugin>
  </extension>

  <asset>
    <texture builtin="gradient" height="100" width="100"
             rgb1="0.6 0.8 1" rgb2="0.1 0.1 0.1" type="skybox"/>
    <texture name="floor_texture" type="2d" builtin="checker"
             width="100" height="100" rgb1="0.1 0.1 0.1" rgb2="0.5 0.5 0.5"/>
    <material name="MatPlane" reflectance="0.5" shininess="1" specular="1"
              texrepeat="10 10" texture="floor_texture"/>
  </asset>

  <worldbody>
    <light cutoff="100" diffuse="0.9 0.9 0.9"
           dir="0 0 -1.3" directional="true"
           exponent="1" pos="0 0 1.3" specular=".1 .1 .1"/>

    <body name="floor" pos="0 0 0">
      <geom name="floor_geom" type="plane" size="10 10 0.1" material="MatPlane"/>
    </body>

    <!-- Agent starts at (0, 0) in XY, height = half-size (0.125) -->
    <body name="agent" pos="0 0 0.125">
      <joint name="j1" type="slide" axis="1 0 0"/>
      <joint name="j2" type="slide" axis="0 1 0"/>
      <!-- new small size 0.125 x 0.125 x 0.125 -->
      <geom name="agent_geom" type="box" size="0.5 0.5 0.5" rgba="0 0 1 1"/>
    </body>
  </worldbody>

  <actuator>
    <!-- PID position controllers for x and y slides -->
    <plugin joint="j1" plugin="mujoco.pid" instance="pid1"/>
    <plugin joint="j2" plugin="mujoco.pid" instance="pid1"/>
  </actuator>
</mujoco>
